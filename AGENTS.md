# Agent Development Notes

## Scope

This repo is the public GitHub profile README for David Batista / Delx. It is a discovery surface, not a runtime package.

## Commands

- No build step.
- Preview by reading `README.md` in GitHub profile rendering.

## Rules

- Keep project counts, stars, downloads, and role claims evidence-based. Refresh live metrics before changing numeric claims.
- Keep the first viewport focused on Delx, local-first MCP servers, and agent infrastructure.
- Do not add secrets, private customer names, private repo names, or unverifiable revenue/user claims.
- Prefer links to canonical public repos, npm packages, and Delx sites.
- Keep the profile useful for humans and agents: concise project table, install commands, and clear ecosystem grouping.

## Update checklist

Before committing profile changes:

1. Verify public repo count and featured project list
2. Verify npm/download numbers if mentioned
3. Check all linked repos still exist and are public
4. Keep image paths relative and committed under `assets/`
